projects/
/state/
# Fleet data is local state, except the standing routing policy, which ships.
/data/*
!/data/routing.json
# The shared package-cache root every goblin builds against.
/caches/
.no-mistakes/
.lavish/
.DS_Store
__pycache__/
*.pyc
.env
config/
!/config/
/config/*
!/config/pipeline.json
/cfo.exe
/frontend/node_modules/

# Harness skill junctions created by install.ps1 -Bootstrap (point at .agents/skills).
/.claude/skills/
/.codex/skills/

# goblin worktrees live beside the checkout they came from
.worktrees/
