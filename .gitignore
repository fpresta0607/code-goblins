projects/
/state/
# Fleet data is local state, except the standing routing policy, which ships.
/data/*
!/data/routing.json
# The shared package-cache root every goblin builds against.
/caches/
.no-mistakes/
.lavish/
.DS_Store
__pycache__/
*.pyc
.env
config/
!/config/
/config/*
!/config/pipeline.json
/cfo.exe
/frontend/node_modules/

# The skill junction install.ps1 -Bootstrap creates (points at .agents/skills);
# /.codex/skills/ is the one earlier bootstraps also made.
/.claude/skills/
/.codex/skills/
# Machine-local Claude permissions; never publish them.
/.claude/settings.json

# goblin worktrees live beside the checkout they came from
.worktrees/
